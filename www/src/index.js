import React from 'react';
import ReactDOM from 'react-dom';

const App = () => {
  return (
    <div>
      <h1>FPGA Emulator GUI</h1>
      {/* TODO: Implement GUI components */}
    </div>
  );
};

ReactDOM.render(<App />, document.getElementById('root'));
